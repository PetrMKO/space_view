import styled, {css} from 'styled-components';

export const AuthWrapper = styled.div`
  width: 690px;
  margin: 0 auto;
  justify-content: center;
`

export const buttonFragment = css({padding:' 17px'})